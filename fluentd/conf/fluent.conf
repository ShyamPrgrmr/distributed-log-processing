<source>
  @type tail
  path /tmp/logs/*.log
  pos_file /tmp/fluentd.pos
  tag tmp.logs
  read_from_head true
  refresh_interval 5

  <parse>
    @type none
  </parse>
  
</source>

<match tmp.logs>
  @type stdout
</match>